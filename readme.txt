1. Make sure you have python3 installed.
2. First you have to install all the requirements so,
    pip install -r requirements.txt

3. You have to provide your email and password in settings.py (line 134, 135). It is used to send the email (for reset password option).

4. Note:- Provide valid email as it will send OTP (valid for 30min) on that email and verify it.

5. In registration page provide valid email as it will send OTP on that email to verify the email.        

5. I try my best to make it more generic. So that it won't be depend on any framework.